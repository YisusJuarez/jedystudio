---
import ContactButton from "../components/ContactButton.astro";
import Container from "../components/Container.astro";
import Nav from "../components/Nav.astro";

const { title, pubDate, description, author, image, tags } = Astro.props;
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="author" content={author} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body class="font-sans text-gray-800 bg-white">
    <Nav />
    <Container>
      <article class="mt-32 mb-32">
        <header class="mb-8">
          <h1 class="text-4xl font-bold mb-2">{title}</h1>
          {
            image?.url && (
              <img
                src={image.url}
                alt={image.alt}
                class="mt-4 rounded-lg shadow"
              />
            )
          }
          {
            tags?.length && (
              <div class="mt-2 flex flex-wrap gap-2">
                {tags.map((tag) => (
                  <span class="bg-blue-100 text-blue-800 text-xs font-medium px-2.5 py-0.5 rounded">
                    #{tag}
                  </span>
                ))}
              </div>
            )
          }
        </header>

        <div class="prose prose-lg max-w-none">
          <slot />
        </div>
      </article>
      <section class="mb-10 mt-32 md:mt-28 md:mb-24">
        <div class="flex justify-center py-4">
          <span
            class="inline max-w-max px-3 py-0.5 border-2 border-purple-500 bg-purple-500 text-white rounded-full font-semibold text-sm shadow-md"
          >
            AGENCIA CREATIVA
          </span>
        </div>
        <h1
          class="text-neutral-800 text-4xl md:text-7xl font-normal tracking-tight w-full text-center text-balance"
        >
          Diseño web que inspira, convierte y hace crecer tu negocio
        </h1>
        <p
          class="text-neutral-500 text-sm font-normal text-center py-10 text-balance max-w-md m-auto"
        >
          Combinamos creatividad y tecnología para desarrollar sitios web que
          dejan huella. Ya sea una landing page o una plataforma, hacemos
          realidad tus ideas.
        </p>
        <div class="flex justify-center">
          <ContactButton />
        </div>
      </section>
    </Container>
  </body>
</html>
